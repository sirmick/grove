# Frequent actions

Recurring requests in this space, auto-maintained by the `ai` assistant. Copy one to re-run it.

## Add a city
**Asked:** 4×

Add a new metro to the `cities` collection: population, founded (negative = BCE), country, and
region, cross-linked with `[[wikilinks]]`. Land it as `_status: review` if any figure is uncertain.

## Refresh stock figures
**Asked:** 2×

Update marketCap, pe, and dividendYield for every record in the `stocks` collection from a current
source, keeping marketCap in USD billions.
