node_modules/
dist/
www/
# spaces/docs is checked in (a grove-managed in-repo space, imported via git subtree). Other
# spaces (e.g. demo) are their own independent git repos, versioned outside this one.
spaces/*
!spaces/docs
spaces/docs/db/
*.log
.env
.DS_Store
.opfs/
packages/app/src/generated/
test-space/
test-results/
playwright-report/
.ingest-test/
screenshots/
